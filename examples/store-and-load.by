func preventConstantOptimisation = first: Int, second: Int -> Int {
    (first + first) * (second + second)
}

func main = argc: Int -> Int {
    let first: Int = 5;
    let x: Int = preventConstantOptimisation(first, 10);
    println_int(x);
    
    x + x
}
