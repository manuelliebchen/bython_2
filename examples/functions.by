func add = x: Int, y: Int -> Int {
    if x < 0 {
        add(x + 1, y - 1)
    } 
    else if x > 0 {
        add(x - 1, y + 1)
    }
    else {
        y
    }
}

func timesFour = x: Int -> Int {
    add(x * 2, x * 2) 
}

func main = argc: Int -> Int {
    timesFour(5)
}
